

query selectUserNotice(userId UserID) UserVo {
  select a.userId as userId, a.email as email, a.nickname as nickname
  from user a, notice b
  where a.userId = b.writerId
  and a.userId = :userId;
}

    
